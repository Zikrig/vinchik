services:
  db:
    image: postgres:16-alpine
    env_file: .env
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U \"$$POSTGRES_USER\" -d \"$$POSTGRES_DB\""]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s

  redis:
    image: redis:7-alpine
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  bot:
    build: .
    env_file: .env
    environment:
      POSTGRES_HOST: db
      # Внутри контейнера всегда 8081 (= правая часть ports). Не путать с хостом 8181.
      WEBHOOK_PORT: "8081"
      WEBHOOK_HOST: "0.0.0.0"
    command: ["python", "bot.py"]
    ports:
      - "8181:8081"
    volumes:
      - ./data:/app/data
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

  web:
    build: .
    env_file: .env
    environment:
      POSTGRES_HOST: db
      WEB_PORT: "8080"
      WEB_HOST: "0.0.0.0"
    command: ["python", "web.py"]
    ports:
      - "8180:8080"
    volumes:
      - ./data:/app/data
    depends_on:
      db:
        condition: service_healthy
      bot:
        condition: service_started
    restart: unless-stopped

volumes:
  pgdata:
